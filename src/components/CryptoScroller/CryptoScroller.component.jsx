/* import React, { useState, useEffect } from "react";
import Ticker from "react-ticker"; */

const CryptoScroller = () => {
  return <div></div>;
  /*   var name = "gg";
  var price = "";
  useEffect(() => {
    fetch("http://localhost:3001/crypto")
      .then((response) => response.json())
      .then((response) => {
        console.log(response);
        name = response[0].name;
        console.log(name);
      });
  }, []);
  const TextFromApi = () => {
    return 1 ? <h1>{name}</h1> : <h1>Placeholder</h1>;
  };

  return (
    <Ticker>
      {({ index }) => (
        <>
          <TextFromApi />
        </>
      )}
    </Ticker>
  ); */
};

export default CryptoScroller;
